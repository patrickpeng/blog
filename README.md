Elysian's Blog.

## Maintenance

This site is built with Jekyll 4 and deployed by GitHub Pages from the `master` branch.

Local build:

```bash
bundle install
bundle exec jekyll build
```

Local preview:

```bash
bundle exec jekyll serve
```

Last maintenance: 2026-04-29
